# ESTIMATES computed by API Evangelist — not published by the provider. See `note` and `replace_by`.
generated: '2026-09-20'
method: estimated
estimator: 'planning/api-economics/estimate_prices.py — design: ESTIMATION.md'
note: ESTIMATES, not quotes. Computed by API Evangelist from the prices this provider publishes on https://aws.amazon.com/gamelift/servers/pricing/
  (transcribed in plans/amazon-gamelift-plans-pricing.yml). Nothing here overrides a published price.
  Each estimate names the figure it came from, the formula, any assumption, and the field the provider
  can publish to replace it — publish that and the estimate is retired.
audience_of_source_pricing: developer-api
pricing_basis: usage-other
estimate_count: 1
estimates:
- kind: entry_cost
  grade: A
  grade_meaning: quoted — the page states this figure
  usd_per_month: 80.0
  meaning: the cheapest published paid tier — a floor on the cost of getting in, NOT the price of the
    API alone
  inputs:
  - file: plans/amazon-gamelift-plans-pricing.yml
    plan: Large scale discount
    price_text: 'Note: we offer pricing discounts for large scale games, please contact us if your game
      costs are over $80K per month'
  replace_by: 'API Commons Plans — plans[].entries[] with type: fee, a numeric `price`, `timeFrame: month`
    and the tier that includes API access stated in `elements`'
