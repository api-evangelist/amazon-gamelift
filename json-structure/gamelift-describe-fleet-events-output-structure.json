{
  "$schema": "https://json-structure.org/meta/core/v0/#",
  "$id": "https://raw.githubusercontent.com/api-evangelist/amazon-gamelift/refs/heads/main/json-structure/gamelift-describe-fleet-events-output-structure.json",
  "name": "DescribeFleetEventsOutput",
  "description": "DescribeFleetEventsOutput schema from Amazon GameLift API",
  "type": "object",
  "properties": {
    "Events": {
      "allOf": [
        {
          "$ref": "#/components/schemas/EventList"
        },
        {
          "description": "A collection of objects containing event log entries for the specified fleet."
        }
      ]
    },
    "NextToken": {
      "allOf": [
        {
          "$ref": "#/components/schemas/NonZeroAndMaxString"
        },
        {
          "description": "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."
        }
      ]
    }
  }
}